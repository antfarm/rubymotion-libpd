rubymotion-libpd
================

A skeleton rubymotion app that interacts with a puredata patch via pd-for-ios and libpd.

To install libpd for iOS, execute ./bin/install_libpd.sh.
Then run rake to start the app.